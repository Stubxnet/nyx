#pragma once
#include "raylib.h"

constexpr float EYES_Y = 1.50f;
constexpr float BODY_HEIGHT = 1.85f;
constexpr float BODY_WIDTH = 0.40;
constexpr float HALF_WIDTH = 0.35f;

constexpr float GRAVITY = 0.05f;

// Atlas parameters
const int TILE = 32;
const int ATLAS_COLS = 2;
const int ATLAS_ROWS = 2;

constexpr int CHUNK_SIZE = 16;